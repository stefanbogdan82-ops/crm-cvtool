def convert_doc_to_docx(doc_path: str) -> str:
    """
    MVP stub.
    Later: call LibreOffice headless in a worker container:
      soffice --headless --convert-to docx --outdir <dir> <file>
    """
    raise NotImplementedError("DOC conversion not enabled in MVP. Please upload DOCX or text-based PDF.")
